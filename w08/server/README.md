EventHub API - Server skeleton

This folder contains a skeleton Express.js server structured in a three-layer architecture: `routes/` (presentation), `services/` (business logic), and `repositories/` (data access).

Start file: `src/index.js` (placeholder).
